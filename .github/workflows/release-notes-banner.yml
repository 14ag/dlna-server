name: Update Latest Release Notes

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  call-release-notes-banner:
    uses: 14ag/dlna-server/.github/workflows/release-notes-banner.yml@main
