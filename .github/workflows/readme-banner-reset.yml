name: Reset Banner Cycle

on:
 workflow_dispatch:

jobs:
 call-workflow:
  uses: 14ag/dlna-server/.github/workflows/readme-banner-reset.yml@main
  with:
   VERCEL_URL: ${{ vars.VERCEL_URL }}
  secrets:
   BANNER_KEY: ${{ secrets.BANNER_KEY }}
